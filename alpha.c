#include<stdio.h>
#include<conio.h>
void main()
{
char a;
clrcsr();
printf("enter the valueof a"):
scanf("%s",a);
}
if( (c>='a' && c<='z') || (c>='A' && c<='Z'))
{
printf("the given a value is an albhabet");
else
printf("given a value is not an albhabet");
getch();
}
